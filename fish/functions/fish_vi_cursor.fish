# The default fish cursor seems to write out strange unicode garbage when
# changing modes. Overriding this function seems to do what I want, which is
# nothing but a normal block cursor.
function fish_vi_cursor; end
